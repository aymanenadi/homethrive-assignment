// Import the Jest types
// I was getting some Jest types errors in my tests, so I added this file to fix them as a quick workaround.
// import '@types/jest';
// require('@types/jest');

process.env.USERS_TABLE = 'users-table';
